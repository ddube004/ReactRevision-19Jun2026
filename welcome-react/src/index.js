// 1. import Area

import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import './App.css';


// 1.1 we do import from  someName location from location library
// 1.2 we do import from  someDefault location from location library

// let define another component

// 2. function defination Area
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
    <App />
);

// 3. export Area
